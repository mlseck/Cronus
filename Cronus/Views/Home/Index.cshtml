
@model   Cronus.ViewModels.HomeViewModel

@{
    ViewBag.Title = "Home Page";
}

<h2>Weekly view</h2>

@Scripts.Render("~/Scripts/index.js")



<h2 id="dateTitle" align="center"></h2>

<div class="col-lg-12">
    <div class="row">
        <div class="col-xs-4">
            <button type="button" width="100px" id="prevWeek" class="btn btn-default pull-left"><span class="glyphicon glyphicon-arrow-left"></span> Previous Week</button>

        </div>
        <div class="col-xs-4">
            <a href="/Home/Monthly" width="100px" class="btn btn-default fc-center"> Go to Monthly view</a>
        </div>
        <div class="col-xs-4">
            <button type="button" width="100px" id="nextWeek" class="btn btn-default pull-right">Next Week <span class="glyphicon glyphicon-arrow-right"></span></button>
        </div>
    </div>
</div>

<br />

@*<table>
    <tr>
        @Html.Partial("HourInputPartial")
    </tr>

    <tr>
        @Html.Partial("HourInputPartial")
    </tr>
</table>*@


<table class="table table-striped">
    <thead>
        <tr>
            <th>Day</th>
            <th></th>
            <th></th>
            <th id="totalHours">Total Hours</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>
                <text class="dayLabel">Monday</text>
                <table class="table table-striped col-md-12" id="actProjSubTableMon">
                    <thead>
                        <tr>
                            <th class="col-md-3">Project</th>
                            <th class="col-md-3">Activity</th>
                            <th class="col-md-1">Hours</th>
                            <th class="col-md-3">Comments</th>
                            <th class="col-md-2"></th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                            <td>
                                <div class="form-group">
                                    <select class="form-control" id="projSelList">
                                        @foreach (DatabaseEntities.project project in Model.Projects)
                                        {
                                            <option>@project.projectName</option>
                                        }
                                    </select>
                                  
                                    
                                </div>
                            </td>
                            <td>
                                <div class="form-group">
                                    <select class="form-control" id="actSelList">
                                        @foreach (DatabaseEntities.activity activity in Model.Activities)
                                            {
                                                 <option>@activity.activityName</option>
                                            }

                                    </select>
                                    
                                </div>
                            </td>
                            <td>
                                <input type="number" min="0" class="form-control hoursInput" id="hoursInputMon" />
                            </td>
                            <td>
                                <input type="text" class="form-control" id="commentsInputMon" />
                            </td>
                            <td>
                                <span class="glyphicon glyphicon-remove" onclick="removeRow(actProjSubTableMon.id, this);" width="200%"></span>
                            </td>
                        </tr>
                    </tbody>
                </table>
                <button type="button" class="add-button" onclick="addNewRows(actProjSubTableMon.id);" name="add">Add Activity</button>

        </tr>
        <tr>
            <td>
                <text class="dayLabel">Tuesday</text>
                <table class="table table-striped col-md-12" id="actProjSubTableTues">
                    <thead>
                        <tr>
                            <th class="col-md-3">Project</th>
                            <th class="col-md-3">Activity</th>
                            <th class="col-md-1">Hours</th>
                            <th class="col-md-3">Comments</th>
                            <th class="col-md-2"></th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                            <td>
                                <div class="form-group">
                                    <select class="form-control" id="projSelList">
                                        @foreach (DatabaseEntities.project project in Model.Projects)
                                        {
                                            <option>@project.projectName</option>
                                        }
                                    </select>
                                    
                                </div>
                            </td>
                            <td>
                                <div class="form-group">
                                    <select class="form-control" id="actSelList">
                                        @foreach (DatabaseEntities.activity activity in Model.Activities)
                                        {
                                            <option>@activity.activityName</option>
                                        }

                                    </select>
                                </div>
                            </td>
                            <td>
                                <input type="number" min="0" class="form-control hoursInput" id="hoursInputTues" />
                            </td>
                            <td>
                                <input type="text" class="form-control" id="commentsInputTues" />
                            </td>
                            <td>
                                <span class="glyphicon glyphicon-remove" onclick="removeRow(actProjSubTableTues.id, this);" width="200%"></span>
                            </td>
                        </tr>
                    </tbody>
                </table>
                <button type="button" class="add-button" onclick="addNewRows(actProjSubTableTues.id);" name="add">Add Activity</button>
            </td>
        </tr>
        <tr>
            <td>
                <text class="dayLabel">Wednesday</text>
                <table class="table table-striped col-md-12" id="actProjSubTableWed">
                    <thead>
                        <tr>
                            <th class="col-md-3">Project</th>
                            <th class="col-md-3">Activity</th>
                            <th class="col-md-1">Hours</th>
                            <th class="col-md-3">Comments</th>
                            <th class="col-md-2"></th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                            <td>
                                <div class="form-group">
                                    <select class="form-control" id="projSelList">
                                        @foreach (DatabaseEntities.project project in Model.Projects)
                                        {
                                            <option>@project.projectName</option>
                                        }
                                    </select>
                                   
                                </div>
                            </td>
                            <td>
                                <div class="form-group">
                                    <select class="form-control" id="actSelList">
                                        @foreach (DatabaseEntities.activity activity in Model.Activities)
                                        {
                                            <option>@activity.activityName</option>
                                        }

                                    </select>
                                </div>
                            </td>
                            <td>
                                <input type="number" min="0" class="form-control hoursInput" id="hoursInputWed" />
                            </td>
                            <td>
                                <input type="text" class="form-control" id="commentsInputWed" />
                            </td>
                            <td>
                                <span class="glyphicon glyphicon-remove" onclick="removeRow(actProjSubTableWed.id, this);" width="200%"></span>
                            </td>
                        </tr>
                    </tbody>
                </table>
                <button type="button" class="add-button" onclick="addNewRows(actProjSubTableWed.id);" name="add">Add Activity</button>
            </td>
        </tr>
        <tr>
            <td>
                <text class="dayLabel">Thursday</text>
                <table class="table table-striped col-md-12" id="actProjSubTableThu">
                    <thead>
                        <tr>
                            <th class="col-md-3">Project</th>
                            <th class="col-md-3">Activity</th>
                            <th class="col-md-1">Hours</th>
                            <th class="col-md-3">Comments</th>
                            <th class="col-md-2"></th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                            <td>
                                <div class="form-group">
                                    <select class="form-control" id="projSelList">
                                        @foreach (DatabaseEntities.project project in Model.Projects)
                                        {
                                            <option>@project.projectName</option>
                                        }
                                    </select>
                            
                                </div>
                            </td>
                            <td>
                                <div class="form-group">
                                    <select class="form-control" id="actSelList">
                                        @foreach (DatabaseEntities.activity activity in Model.Activities)
                                        {
                                            <option>@activity.activityName</option>
                                        }

                                    </select>
                                </div>
                            </td>
                            <td>
                                <input type="number" min="0" class="form-control hoursInput" id="hoursInputThu" />
                            </td>
                            <td>
                                <input type="text" class="form-control" id="commentsInputThu" />
                            </td>
                            <td>
                                <span class="glyphicon glyphicon-remove" onclick="removeRow(actProjSubTableThu.id, this);" width="200%"></span>
                            </td>
                        </tr>
                    </tbody>
                </table>
                <button type="button" class="add-button" onclick="addNewRows(actProjSubTableThu.id);" name="add">Add Activity</button>
            </td>
        </tr>
        <tr>
            <td>
                <text class="dayLabel">Friday</text>
                <table class="table table-striped col-md-12" id="actProjSubTableFri">
                    <thead>
                        <tr>
                            <th class="col-md-3">Project</th>
                            <th class="col-md-3">Activity</th>
                            <th class="col-md-1">Hours</th>
                            <th class="col-md-3">Comments</th>
                            <th class="col-md-2"></th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                            <td>
                                <div class="form-group">
                                    <select class="form-control" id="projSelList">
                                        @foreach (DatabaseEntities.project project in Model.Projects)
                                        {
                                            <option>@project.projectName</option>
                                        }
                                    </select>
                                   
                                </div>
                            </td>
                            <td>
                                <div class="form-group">
                                    <select class="form-control" id="actSelList">
                                        @foreach (DatabaseEntities.activity activity in Model.Activities)
                                        {
                                            <option>@activity.activityName</option>
                                        }

                                    </select>
                                </div>
                            </td>
                            <td>
                                <input type="number" min="0" class="form-control hoursInput" id="hoursInputFri" />
                            </td>
                            <td>
                                <input type="text" class="form-control" id="commentsInputFri" />
                            </td>
                            <td>
                                <span class="glyphicon glyphicon-remove" onclick="removeRow(actProjSubTableFri.id, this);" width="200%"></span>
                            </td>
                        </tr>
                    </tbody>
                </table>
                <button type="button" class="add-button" onclick="addNewRows(actProjSubTableFri.id);" name="add">Add Activity</button>
            </td>
        </tr>
        <tr>
            <td>
                <text class="dayLabel">Saturday</text>
                <table class="table table-striped col-md-12" id="actProjSubTableSat">
                    <thead>
                        <tr>
                            <th class="col-md-3">Project</th>
                            <th class="col-md-3">Activity</th>
                            <th class="col-md-1">Hours</th>
                            <th class="col-md-3">Comments</th>
                            <th class="col-md-2"></th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                            <td>
                                <div class="form-group">
                                    <select class="form-control" id="projSelList">
                                        @foreach (DatabaseEntities.project project in Model.Projects)
                                        {
                                            <option>@project.projectName</option>
                                        }
                                    </select>
                                   
                                </div>
                            </td>
                            <td>
                                <div class="form-group">
                                    <select class="form-control" id="actSelList">
                                        @foreach (DatabaseEntities.activity activity in Model.Activities)
                                        {
                                            <option>@activity.activityName</option>
                                        }

                                    </select>
                                </div>
                            </td>
                            <td>
                                <input type="number" min="0" class="form-control hoursInput" id="hoursInputSat" />
                            </td>
                            <td>
                                <input type="text" class="form-control" id="commentsInputSat" />
                            </td>
                            <td>
                                <span class="glyphicon glyphicon-remove" onclick="removeRow(actProjSubTableSat.id, this);" width="200%"></span>
                            </td>
                        </tr>
                    </tbody>
                </table>
                <button type="button" class="add-button" onclick="addNewRows(actProjSubTableSat.id);" name="add">Add Activity</button>
            </td>
        </tr>
        <tr>
            <td>
                <text class="dayLabel">Sunday</text>
                <table class="table table-striped col-md-12" id="actProjSubTableSun">
                    <thead>
                        <tr>
                            <th class="col-md-3">Project</th>
                            <th class="col-md-3">Activity</th>
                            <th class="col-md-1">Hours</th>
                            <th class="col-md-3">Comments</th>
                            <th class="col-md-2"></th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                            <td>
                                <div class="form-group">
                                    <select class="form-control" id="projSelList">
                                        @foreach (DatabaseEntities.project project in Model.Projects)
                                        {
                                            <option>@project.projectName</option>
                                        }
                                    </select>
                                    
                                </div>
                            </td>
                            <td>
                                <div class="form-group">
                                    <select class="form-control" id="actSelList">
                                        @foreach (DatabaseEntities.activity activity in Model.Activities)
                                        {
                                            <option>@activity.activityName</option>
                                        }

                                    </select>
                                </div>
                            </td>
                            <td>
                                <input type="number" min="0" class="form-control hoursInput" id="hoursInputSun" />
                            </td>
                            <td>
                                <input type="text" class="form-control" id="commentsInputSun" />
                            </td>
                            <td>
                                <span class="glyphicon glyphicon-remove" onclick="removeRow(actProjSubTableSun.id, this);" width="200%"></span>
                            </td>
                        </tr>
                    </tbody>
                </table>
                <button type="button" class="add-button" onclick="addNewRows(actProjSubTableSun.id);" name="add">Add Activity</button>
            </td>
        </tr>
    </tbody>
</table>
<button type="button" width="100px" id="save" class="btn btn-default pull-left">Save <span class="glyphicon glyphicon-check"></span></button>
<br />
<br />
<br />
<br />
<br />



